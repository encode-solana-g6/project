import { css } from "../../styled-system/css";

export const col = css.raw({
  display: "flex",
  flexDirection: "column",
});

export const row = css.raw({
  display: "flex",
  flexDirection: "row",
});
